# Skill index

`skills/manifest.json` is the machine-readable index. The initial pack intentionally mirrors the auto-research lifecycle while specializing the technical skills for `llm-agent-cl`.

## Workflow

- `search-roam` — load the smallest canonical research context.
- `save-research` — create sourced Org research with required metadata.
- `source-verification` — prefer primary sources and track retrieval dates.
- `branch-workflow` — reconcile state and isolate changes.
- `implement-one-design` — future one-slot implementation handoff.
- `mark-implemented` / `mark-rejected` — terminal ledger transitions.

## Design and parity

- `create-design-file`
- `review-design`
- `design-to-issues`
- `feature-parity`
- `provider-research`
- `model-contract`
- `transport-contract`
- `streaming-contract`
- `tool-contract`
- `mcp-contract`
- `agent-runtime-contract`
- `production-readiness`
